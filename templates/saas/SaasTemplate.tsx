import { Container, Section } from "@/components/layouts";

export function SaasTemplate() {
  return (
    <Section>
      <Container>
        <h2 className="text-2xl font-bold">SaaS Template</h2>
        <p className="mt-3 text-zinc-600">Hero, features, pricing, and CTA sections.</p>
      </Container>
    </Section>
  );
}
