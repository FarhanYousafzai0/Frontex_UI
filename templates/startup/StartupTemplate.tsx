import { Container, Section } from "@/components/layouts";

export function StartupTemplate() {
  return (
    <Section>
      <Container>
        <h2 className="text-2xl font-bold">Startup Template</h2>
        <p className="mt-3 text-zinc-600">Launch-focused layout with metrics and CTA.</p>
      </Container>
    </Section>
  );
}
